import { Paper, colors } from '@mui/material';
import { styled } from '@mui/material/styles';

const StyledPaperUniversal = styled(Paper)({
  backgroundColor: colors.yellow[200],
  height: '100vh',
  padding: '50px',
});

export default StyledPaperUniversal;
